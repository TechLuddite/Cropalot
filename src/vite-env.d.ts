/// <reference types="vite/client" />

/** Git commit this bundle was built from, injected at build time by vite.config.ts. */
declare const __COMMIT_SHA__: string;

/** ISO 8601 timestamp of the build, injected at build time by vite.config.ts. */
declare const __BUILD_TIME__: string;
